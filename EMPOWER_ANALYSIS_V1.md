# Empower Analysis V1｜赋能分析算法实现规格

状态：当前实现规范。受 `PRODUCT_SPEC_V2.md` 产品宪法约束；本文件不能覆盖或修改宪法。

## 目标

赋能不能停在“召回历史”。算法必须把历史转成对当前选择有用的个人决策依据，并同时回答两类问题：

1. **过去的决策如何影响当前决策？**
2. **过去的认知平面与当前/T0相比，发生了什么变化；当前能力、资源与执行条件还缺什么？**

最终选择仍由用户完成。算法不得输出“你应该选 A/B”。

## 输入

- 当前四项：发生了什么、为什么现在必须选择、当前有哪些选择、卡在哪里；
- 当前 8 个结构维度：领域、角色、目标、约束/资源、选项、信息、社会关系、技术条件；
- Sanity Context / Knowledge Base 召回候选；
- 结构化相似度重排结果；
- 历史 Event / Decision Node；
- 历史 Cognition Plane；
- T0 当前认知基线；
- 用户当前已分类的能力、资源、工具、现金流与不确定性证据。

## 输出一：当前决策完整度

显示 8 个结构维度里哪些已经明确、哪些仍缺失。缺失项不是自动补全，而是当前决策在行动前需要补足的事实。

当前真实测试：**7 / 8 已明确，缺“角色与责任位置”。**

## 输出二：决策改进动作

这是“赋能”的核心之一。它不是选择答案，而是从用户自己的历史中生成当前决策流程必须改进的动作。

当前规则包括：

- **补齐角色/责任位置**：当前结构缺失时强制提示；
- **验证规则**：当相似历史的后来复盘明确出现“没想太多 / 简单认为 / 选择错了 / 冲动 / 偏执”等用户原话时，把它转成当前的验证条件，要求先定义观察期、验证指标和停止条件；
- **分阶段投入**：历史存在明确资金/学习投入，且当前有现金流/可支撑时间证据时，要求设置投入上限、验证周期、退出条件；
- **旧路不自动安全**：历史出现“回到旧路”且后续结果继续下降时，当前若把旧路作为退路，必须重新验证它是否仍满足生存目标；
- **执行准备度**：当前原话存在“工具不会用 / 第一步不知道 / 方向未定”时，要求先形成可执行清单，再比较方案。

每一条动作都必须带历史事件或当前原话来源。

## 输出三：过去决策 → 当前决策影响卡

每一个相似事件必须显示：

- 为什么与当前有关；
- 当时怎么选；
- 当时为什么；
- 后来结果；
- 后来怎么看；
- 对这次决策的检查点；
- 来源事件。

算法只建立关联与检查点，不声称历史原因必然导致结果。

## 输出四：认知赋能

认知变化不等于成长评分。

算法分开显示：

- 已确认变化；
- 保持相同；
- 当前有记录 / 历史无记录；
- 历史有记录 / 当前无记录；
- 证据不足。

并额外计算可验证的结构变化，例如**可见选项数量**。

当前真实测试：当前明确列出 **4 条路径**；相似历史里最多明确记录 **2 条路径**。因此只能说“可见选项数量扩大”，不能说整体认知更高。

## 输出五：能力赋能

能力、认知、资源必须分开。

当前能力/执行层读取已分类的 T0 原话，例如：

- `先做什么不知道。`
- `我现在需要很多的工具，但是我又不会用。`
- `都从来没有真正地用过，只用它聊天……`

当前资源层读取已分类事实，例如：

- `因为这是我最后的积蓄了。`
- `以现在毫无收入来说，我最多还能坚持一年。`

因此能力赋能可以明确指出：先把方向、第一步、工具能力、投入规则做成可执行条件，而不是只比较想法。

## 输出六：认知平面比较

用正式 Cognition Plane 20 维比较历史 ↔ T0：

`worldScope / cognitionRadius / informationSources / technologyAccess / socialSampleRange / riskModel / opportunityModel / selfModel / relationshipModel / moneyModel / workModel / learningModel / failureModel / timeHorizon / actionStyle / uncertaintyHandling / visibleOptionBreadth / executionCapacity / resourceCapacity / physicalOrMemoryConstraints`

只使用已有记录和来源。不补值，不生成成长分。

## 当前真实验收

- `retrievalMode = context`
- Context 当前召回：**18 条**
- 结构化保留：**4 条**
- 当前决策结构：**7 / 8**
- 可见选项：**当前 4 / 历史最多 2**
- 匹配历史认知平面：**4 个**
- 正式平面已确认变化：**2 项**
- 决策改进动作：**5 条**

五条当前动作：

1. 先补齐当前角色与责任位置；
2. 把“凭早期感觉下结论”改成“先定义验证条件”；
3. 把投入改成分阶段验证，而不是一次性押注；
4. 旧路不能自动当作“安全退路”；
5. 先把方向变成可执行的第一步。

## 禁止项

- 不输出唯一最终选择；
- 不把历史相似度当成功概率；
- 不把“后来”自动等于“更好”；
- 不把认知、能力、资源混成一个分数；
- 不把用户原话改写成系统事实；
- 不生成缺失历史；
- 不做因果判决。
