# Tell Me More archive Studio

Read-only archive viewer for six structured document types. Creation and sealing
go through the application service; Studio mutation actions are disabled.

From the repository root: `npm run studio` or `npm run schema:deploy`.
See [the project README](../README.md) for credentials, unclaimed-project ownership,
Context authorization, privacy boundaries, and the demo walkthrough.
