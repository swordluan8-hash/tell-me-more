# Explicit fallback. Set sanity after provisioning; never put tokens here.
TMM_STORAGE=local-demo
# Context credentials belong in sanity/.env.local and stay server-side.

# Public judge deployment: synthetic read-only data + fixed non-persistent Context preview.
TMM_PUBLIC_DEMO=false
